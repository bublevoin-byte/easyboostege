{
  "name": "easyboost-server",
  "version": "1.0.0",
  "description": "Backend for Easy Boost — English EGE tutor app",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:production": "npm run db:migrate && node server.js",
    "check": "node --check server.js && node --check config.js && node --check db.js && node --check ai/content.js && node --check ai/speaking.js && node --check ai/provider-control.js && node --check public/api.js && node --check public/sync.js && node --check public/router.js && node --check public/app.js && node --check public/privacy.js && node --check public/tts.js && node --check public/pwa.js && node --check public/service-worker.js && node --check security/csp.js && node --check scripts/postgres-backup.js && node --check scripts/postgres-restore.js",
    "test": "node --test",
    "audit:production": "npm audit --omit=dev --audit-level=high",
    "security:secrets": "node scripts/scan-secrets.js",
    "db:migrate": "node scripts/migrate.js",
    "db:import-json": "node scripts/import-json.js",
    "db:backup": "node scripts/postgres-backup.js",
    "db:restore": "node scripts/postgres-restore.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.2",
    "msedge-tts": "^2.0.7",
    "pg": "^8.22.0",
    "zod": "^4.4.3"
  }
}
