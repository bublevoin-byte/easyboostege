# 01 — Зафиксировать базовую линию производительности

Status: ready-for-agent
Blocked by: —
Spec: .scratch/frontend-performance-vite/spec.md#2-измеренная-базовая-линия

## Что сделать

Записать измеренные показатели раздела 19 в `docs/PERFORMANCE_BASELINE.md`, чтобы улучшение после
ленивой загрузки можно было доказать числами, а не впечатлением. Файл описывает, чем и на чём
мерили, что получилось и какое значение считается целевым.

Замер уже выполнен 29 июля 2026 года командой `npm run test:e2e:performance` на Windows в headless
Chrome:

```
LCP = 44 мс (бюджет 2500)          — в бюджете
CLS = 0.000 (бюджет 0.1)           — в бюджете
INP = 224 мс (бюджет 200)          — ПРЕВЫШЕН
индикатор проверки ИИ = 34 мс      — в бюджете
JavaScript по сети = 350 КБ
взаимодействия по убыванию: 224, 112, 24, 16, 16 мс
```

## Границы

- Только новый документ. Код не меняется, тесты не меняются.
- Не переписывать бюджеты в `e2e/performance.test.js` — это делает тикет 07.

## Файлы

- `docs/PERFORMANCE_BASELINE.md` — новый.

## Definition of Done

- [ ] Документ содержит дату, команду, окружение, таблицу показателей и целевые значения.
- [ ] Документ честно называет INP превышенным и объясняет причину одной фразой.
- [ ] `npm run lint` проходит.
- [ ] Один коммит.
