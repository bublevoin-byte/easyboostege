# 05 — Активный банк частей речи

Status: done
Blocked by: 03 — Активный runner
Spec: `.scratch/grammar-2/spec.md#implementation-decisions`

## Что сделать

Расширить степени сравнения, местоимения, числительные, множественное число, -ing/-ed adjectives и наречия до полноценной четырёхуровневой тренировки с устойчивыми ID, объяснениями и точной работой над формой/согласованием.

## Границы

- Входят шесть названных тем, 144 задания, irregular forms и targeted error focus.
- Не входят времена, глагольные конструкции, служебные слова и mixed/adaptive orchestration.

## Файлы

- `public/grammar-catalog-content.js`, `public/grammar-catalog.js` — контент и coverage.
- `public/modules/grammar.js`, `public/screens/grammar.js` — общий runner и targeted result.
- `test/`, `e2e/` — catalog/accessibility/regression contracts.

## Definition of Done

- [x] Шесть тем содержат минимум 144 уникальных задания, по 6 каждого типа на тему.
- [x] Irregular forms и допустимые варианты явно перечислены и автоматически проверяются.
- [x] Correction/transform требуют активного восстановления, а не выбора из тех же ответов.
- [x] Слабость сохраняется на уровне темы и error code и попадает в следующий targeted set.
- [x] Coverage, accessibility и regression tests проходят.
- [x] `npm test`, `npm run lint` и `npm run check` проходят; один коммит на тикет.

## Freeze evidence

- Финальный v12 review remediation прошёл RED `1 total / 0 pass / 1 fail` → GREEN `1/1`: executable OpenAPI pre-activation envelope повторяет точный runtime retry-order по каждому immutable Grammar 1 item — одиночная ошибка запрещена; первая ошибка из двух имеет `transferStatus: null`; правильный повтор остаётся с `null`; второй промах обязан иметь `due_next_session`; misplaced/missing due отклоняются. Positive/negative matrix покрывает `choice` и `input` во всех шести активированных темах, текущие active-схемы не изменены. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `96/96`; полный unit suite — `1478 total / 1436 pass / 42` штатных PostgreSQL skip / `0 fail`. Lint; generated OpenAPI check; check (`372 JS`, `205` handlers, `123` names); build (`482` assets, `588.2 КБ`, `9` lazy chunks); последовательные full+adaptive Chromium E2E; secrets `1121`; history `304`; audit `0`; diff-check green. Последний disposable PostgreSQL project `easyboost-postgres-integration-30360` с миграциями `001–052`, `42/42` и полной cleanup остаётся применимым: после него менялись только OpenAPI и тесты; Docker остановлен. Текущий v2 fingerprint `fnv1a32:86530c23`, immutable v1 — `fnv1a32:45cee292`. Provider/платных вызовов, package install, commit, push и deploy не было; тикет остаётся `in-progress` до двух свежих независимых буквальных `ZERO_FINDINGS` и одного локального коммита.
- Финальный v11 review remediation прошёл RED `1 total / 0 pass / 1 fail` → GREEN `1/1`: отдельные OpenAPI-схемы queued pre-activation Grammar 1 outcomes теперь связывают `choice` только с историческим `construction_choice`, а `input` — только с `word_or_verb_form`, сохраняя exact null diagnostic/confusion и bounded retry contract общего legacy item. Исполняемая positive/negative matrix покрывает оба типа во всех шести активированных темах и подтверждает parity с runtime, не ослабляя текущие active-схемы. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `96/96`; полный unit suite — `1478 total / 1436 pass / 42` штатных PostgreSQL skip / `0 fail`. Lint; generated OpenAPI check; check (`372 JS`, `205` handlers, `123` names); build (`482` assets, `588.2 КБ`, `9` lazy chunks); full+adaptive Chromium E2E; secrets `1121`; history `304`; audit `0`; diff-check green. Последний disposable PostgreSQL project `easyboost-postgres-integration-30360` с миграциями `001–052`, `42/42` и полной cleanup остаётся применимым: после него менялись только OpenAPI и тесты; Docker остановлен. Текущий v2 fingerprint `fnv1a32:86530c23`, immutable v1 — `fnv1a32:45cee292`. Provider/платных вызовов, package install, commit, push и deploy не было; тикет остаётся `in-progress` до двух свежих независимых буквальных `ZERO_FINDINGS` и одного локального коммита.
- Финальный v10 review remediation прошёл RED `1 total / 0 pass / 1 fail` → GREEN `1/1`: `core.g.10.transform.5` теперь требует буквальную конструкцию `The first box is as heavy as ...`, а конечное множество ответов `core.g.10.transform.8` буквально соблюдает требуемое начало `Team D has` и один из трёх опубликованных endings. Prompt и grader больше не расходятся на `weighs as much as` или fronted `Of the four teams`. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `96/96`; текущий v2 fingerprint `fnv1a32:86530c23`, immutable v1 — `fnv1a32:45cee292`.
- Финальный v9 review remediation прошёл RED `1 total / 0 pass / 1 fail` → GREEN `1/1`: три controlled transform prompt больше не задают открытое множество одинаково правильных парафразов, а буквально ограничивают конечные автоматически проверяемые структуры. Поэтому грамматичные, но не указанные конструкции вроде `the other box`, `of all the hotels` и `of all four teams` больше не выглядят требуемыми ответами и не превращаются в ложные mastery errors. Stale summary fingerprint удалён. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `96/96`; v2 fingerprint на том checkpoint — `fnv1a32:a1becaae`, immutable v1 — `fnv1a32:45cee292`.
- Финальный v8 review remediation прошёл exact RED `5 total / 0 pass / 5 fail` → GREEN `5/5`: настоящий browser review для migrated active input публикует current exact weakness, а runtime и catalog-generated OpenAPI принимают как этот current tuple, так и ограниченный исторический pre-activation fallback, не разрешая null diagnostic новым active choices. Тот же OpenAPI positive gate принимает целый active session с linked current independent error; deduplicated branches и exact nullable-string schemas сохраняют взаимоисключающий `oneOf`. Controlled transforms теперь также принимают конечные грамматичные варианты `Hotel C is the worst of the hotels in the list.`, `The first box is as heavy as the other.` и `Of the four teams, Team D has the least experience.` Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `96/96`; v2 fingerprint на том checkpoint — `fnv1a32:6d5c7ced`, immutable v1 — `fnv1a32:45cee292`.
- Публичный TDD-контракт был зафиксирован до production как `4 total / 1 pass / 3 fail`, затем закрыт GREEN; дополнительный аудит однозначности transform-подсказок прошёл RED `9 total / 8 pass / 1 fail` → GREEN `9/9`. После первого независимого review remediation exact weakness transfer, finite equivalents, однозначных choice-контекстов и pre-activation legacy compatibility прошёл RED `5 total / 0 pass / 5 fail` → GREEN `5/5`. Следующий независимый review нашёл расхождения исполняемого OpenAPI с active diagnostic/topic/type boundary и четыре неоднозначных либо неполных content surface; все public seams были одновременно воспроизведены exact RED `4 total / 0 pass / 4 fail` и закрыты GREEN `4/4`. Ещё один независимый review выявил cross-topic active OpenAPI envelope и пропущенный source-preserving вариант `exactly three hundred`; оба seam прошли RED `2 total / 0 pass / 2 fail` → GREEN `2/2`. Следующий review выявил пропущенный active whitelist для `GrammarIndependentError` и отсутствие topic ownership для session/review diagnostics: exact RED `1 total / 0 pass / 1 fail` закрыт GREEN `1/1`. Следующий review нашёл same-topic cross-item diagnostic подмену и две границы очередей Grammar 1: exact RED `4 total / 0 pass / 4 fail` закрыт GREEN `4/4`. Финальный v6 review выявил допустимый null diagnostic у нового active choice, несвязанные item/diagnostic/reason/confusion tuples, независимую ошибку без соответствующего wrong session outcome, вымышленный `c2` pointer и пропущенный source-preserving вариант `Hotel C is the worst in the list.`; точный RED `3 total / 0 pass / 3 fail` закрыт GREEN `4/4` вместе с positive pre-activation compatibility guard. Catalog-generated OpenAPI теперь воспроизводимо перечисляет все точные built-in IDs и все `408` active diagnostic tuples, а session extension связывает `independentError` с одним буквальным wrong outcome. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `93/93`.
- Темы `10`, `11`, `12`, `16`, `17`, `20` содержат `192` уникальных активных задания: ровно `32` на тему и `8` каждого из типов `choice`, `input`, `correction`, `transform`. Current `grammar-core-v2` содержит `584` упражнения + `18` exam gaps, fingerprint синхронизирован с финальным gate выше; immutable v1 остаётся `200 + 18`, `fnv1a32:45cee292`.
- Общий Ticket03 runner без тематических веток строит `16` originals и выдаёт до `16` отдельных exact paired transfers; выбранный wrong-option diagnostic теперь буквально ограничивает transfer точным `errorCode` + `confusionPair` и fail-closed отклоняет поддельную слабость. Exhaustive gate проверяет все `192` задания, все `48` choices и `144` wrong-option diagnostics, reconstructive correction/transform без options, явные irregular/finite answers, `256` deterministic seeds на тему, clean server envelope до `learned`, review due `+1 day`, Voice parity и offline shell. Версионированный общий контракт сохраняет только очереди Grammar 1 для шести тем, которые активирует Ticket05, и отклоняет новые Ticket05 pointers под видом `legacy_practice`; browser restore, runtime schema и исполняемый OpenAPI проверяют одну границу.
- Полный unit/integration suite: `1478 total / 1436 pass / 42` штатных PostgreSQL skip / `0 fail`. Disposable PostgreSQL project `easyboost-postgres-integration-30360` применил миграции `001–052`, прошёл `42/42`, включая общий Grammar mastery persistence/replay/conflict/export/delete contract, затем полностью удалил container/volume/network; независимые elevated post-run filters подтвердили пустые project resources.
- Lint; generated OpenAPI check; check (`372 JS`, `205` handlers, `123` names); build (`482` assets, `588.2 КБ` shell JS, `9` lazy chunks); full и adaptive Chromium E2E; secret scan `1121`; history scan `304`; production audit `0 vulnerabilities` и `git diff --check` зелёные.
- Docker остановлен. Provider/платных вызовов, package install, push и deploy не было. Ticket05 закрывается `done` одним локальным коммитом после двух свежих независимых буквальных `ZERO_FINDINGS`; Ticket06 не начинался.
