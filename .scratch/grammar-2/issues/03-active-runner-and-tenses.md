# 03 — Активный runner и полноценные времена

Status: done
Blocked by: 01 — Каталог; 02 — честное освоение
Spec: `.scratch/grammar-2/spec.md#implementation-decisions`

## Что сделать

Запустить новый четырёхуровневый runner на пяти темах времён. Каждая тема получает минимум 24 встроенных задания: choice, input, correction и controlled transform/translation; ошибки ведут к разбору и новому transfer item, а не только к повтору того же вопроса.

## Границы

- Входят общий runner, четыре типа задания и минимум 120 заданий для пяти тем времён.
- Не входят банки глагольных конструкций, частей речи и служебных слов из тикетов 04–06.

## Файлы

- `public/grammar-catalog-content.js`, `public/grammar-catalog.js` — времена и контракты четырёх типов.
- `public/modules/grammar.js`, `public/screens/grammar.js` — runner и интерфейс.
- `test/`, `e2e/` — доменные и browser-проверки.

## Definition of Done

- [x] Пять тем времён содержат минимум 120 уникальных автоматически проверенных заданий, по 6 каждого типа на тему.
- [x] Runner одинаково проверяет четыре типа, нормализует допустимые ответы и сохраняет точный error code/confusion pair.
- [x] Открытие правила помечает evidence assisted; assisted session не продвигает stage.
- [x] Ошибка выдаёт объяснение и отдельное неповторяющееся transfer-задание той же слабости.
- [x] Mobile/desktop browser flow доказывает четыре типа, reload, stage и доступность.
- [x] `npm test`, `npm run lint` и `npm run check` проходят; один коммит на тикет.

## Current implementation inventory

- Final6 closeout: независимые Standards и Spec review вернули буквальный `ZERO_FINDINGS` на одной frozen identity — base `5b8dd066cfa7a8e0746148bbf032e3bf4b66c316`, 36 путей, canonical manifest 3657 bytes, SHA-256 `eda67bf778ee95fceee5b2072176f2f4bde481a9b3507206f16b35a28c38e8c6`. Тикет закрыт `done` одним локальным коммитом; push и deploy не выполнялись.
- Final5 review remediation заменяет предыдущие freeze figures ниже; статус тикета остаётся `in-progress` до двух новых буквальных `ZERO_FINDINGS` и одного локального коммита. Exact TDD прошёл RED `112 total / 107 pass / 5 fail` → GREEN `112/112`: локальная очередь и `_conflictRevision` больше не считаются durable evidence, поэтому `completion_pending` очищается только после server `applied`, exact `replay` или совпадающей canonical server history. Первый конфликтный exact event/UUID/material переживает coalescing, reload и новые same-topic попытки; более позднее событие темы блокируется за ним и не может стереть исходное доказательство или повторно изменить статистику. Ровно два v2 input prompt (`core.g.2.f.6/.7`) теперь буквально требуют action in progress, исключая допустимое Past Simple чтение; immutable v1 не изменён, новый v2 fingerprint `fnv1a32:2e59251e`, v1 `fnv1a32:45cee292`. OpenAPI содержит исполняемое exact `session.items` → `typeScores` ограничение для clean/assisted active и legacy evidence, а clean active branch дополнительно структурно требует `4/4` по каждому из четырёх типов; mismatched correct/total payload отклоняется schema evaluator. Affected-контур прошёл `120/120`, полный unit — `1437 total / 1395 pass / 42` штатных PostgreSQL skip / `0 fail`; disposable PostgreSQL project `easyboost-postgres-integration-18688` применил миграции `001–052`, прошёл `42/42` и полностью удалил container/volume/network. Lint; check (`367 JS`, `205` handlers, `123` names); build (`482` assets, `502.6 КБ` shell JS, `9` lazy chunks); full+adaptive Chromium E2E; secrets `1113`; history `302`; production audit `0 vulnerabilities` и `git diff --check` зелёные. Docker остановлен; provider/платных вызовов, install, commit, push и deploy не было.
- Final4 review remediation заменяет предыдущие freeze figures ниже; статус тикета остаётся `in-progress` до двух новых буквальных `ZERO_FINDINGS` и одного локального коммита. Exact bounded TDD прошёл RED `112 total / 107 pass / 5 fail` → GREEN `112/112`: `gStart` и `gReview` больше не могут перезаписать локальный `completion_pending`, а детерминированно восстанавливают и повторяют exact event/UUID/outcomes до durable apply/replay; stale CAS сохраняет исходный UUID и compare-and-set evidence в bounded conflict marker вместо создания нового события и повторного изменения статистики. Replay authority теперь сравнивает exact canonical `canonical-json-v1` material всего события; 32-битный FNV digest не даёт replay authority. Два разных runtime-valid legacy event с одним UUID и известной коллизией `fnv1a32:34dfbaf5` (`startedAt` `19181966713209` и `288105508095880`) различаются новым контрактом, а file/PostgreSQL применяют первое, отклоняют второе как conflict и сохраняют одну запись истории. OpenAPI структурно требует только correct/null-weakness outcomes у unassisted session и связывает built-in pointer kind `.c/.c2/.f/.correction/.transform` с exact runtime type; positive/negative payload проверяются dependency-free YAML parser + исполняемым JSON-Schema subset evaluator, а не текстовыми regex-предположениями. Расширенный Grammar-контур прошёл `216/216`, content/property/Voice — `27/27`, полный unit — `1434 total / 1392 pass / 42` штатных PostgreSQL skip / `0 fail`; disposable PostgreSQL project применил миграции `001–052`, прошёл весь suite `42/42`, включая общий collision replay/file/PG contract, и полностью удалил container/volume/network. Lint; check (`367 JS`, `205` handlers, `123` names); build (`482` assets, `502.9 КБ` shell JS, `9` lazy chunks); full+adaptive Chromium E2E; secrets `1113`; history `302`; production audit `0 vulnerabilities` и `git diff --check` зелёные. Docker остановлен; fingerprints каталога неизменны: v2 `fnv1a32:74ce379c`, v1 `fnv1a32:45cee292`. Provider/платных вызовов, install, commit, push и deploy не было.
- Final3 review remediation заменяет предыдущие freeze figures ниже; статус тикета остаётся `in-progress` до двух новых буквальных `ZERO_FINDINGS` и одного локального коммита. Exact browser/OpenAPI RED `32 total / 30 pass / 2 fail` закрыт тем же контуром GREEN `32/32`. Обычная кнопка «К темам» больше не уничтожает локальный `completion_pending`: при недоказанной durability она сохраняет exact event/UUID/ordered outcomes для последующего reload и replay, а очистка остаётся привязана к durable apply/exact replay. OpenAPI active built-in wrong outcomes теперь разделены на четыре исполняемые ветви `choice|text × original|transfer`: wrong choice требует непустой server-owned `diagnosticId`, wrong input/correction/transform требует exact `null`; положительные и два cross-type отрицательных payload проверяются непосредственно по структурным ветвям документа. Расширенный Grammar-контур повторно прошёл `213/213`, content/property/Voice — `27/27`, полный unit — `1431 total / 1389 pass / 42` штатных PostgreSQL skip / `0 fail`; lint, check (`366 JS`, `205` handlers, `123` names), build (`482` assets, `503.3 КБ` shell JS, `9` lazy chunks), full+adaptive Chromium E2E, secrets `1113`, history `302`, production audit `0 vulnerabilities` и `git diff --check` зелёные. Последний live PostgreSQL `42/42` с миграциями `001–052` остаётся точным: после него менялись только browser navigation, OpenAPI и их тесты, server validation/repository path не менялся; Docker остаётся остановлен. Fingerprints неизменны: v2 `fnv1a32:74ce379c`, v1 `fnv1a32:45cee292`. Provider/платных вызовов, install, commit, push и deploy не было.
- Последний late-error/OpenAPI remediation checkpoint заменяет более ранние числовые freeze figures ниже; статус тикета остаётся `in-progress` до двух новых буквальных `ZERO_FINDINGS` и одного локального коммита. Exact TDD RED `55 total / 48 pass / 7 fail` закрыт тем же focused-командным контуром GREEN `55/55`. Неправильный ответ, самостоятельно зафиксированный до показа объяснения, теперь несёт только server-verifiable `independentError` (`itemId`, exact authored `diagnosticId|null`, один из семи `reason`, exact `confusionPair|null`): последующее автоматическое раскрытие ответа по-прежнему делает всю session/review assisted и не позволяет продвинуть mastery, но точная поздняя ошибка консервативно понижает уже освоенную тему не более чем на одну стадию и ставит её due. Помощь до ответа, generated content, несовпадающий catalog pointer или произвольный client reason не дают права на регрессию. Browser reload сохраняет это различие без learner answer/content, file/PostgreSQL используют тот же validation/reducer contract. OpenAPI теперь структурно совпадает с runtime: builtin active outcome и generated active/legacy outcome разделены exact correct/wrong `oneOf`/`allOf` ветвями, поэтому contradictory `correct/errorCode/diagnosticId/confusionPair/transferStatus` payload нельзя объявить допустимым. Расширенный Grammar/catalog/mastery/offline/owner/Voice/security контур прошёл `213/213`, отдельный content/property/Voice — `27/27`, полный unit — `1431 total / 1389 pass / 42` штатных PostgreSQL skip / `0 fail`. Disposable PostgreSQL project `easyboost-postgres-integration-19004` применил миграции `001–052`, прошёл `42/42`, включая общий late-error/file/PG контракт, и полностью удалил container/volume/network; Docker остановлен. Lint; check (`366 JS`, `205` handlers, `123` names); build (`482` assets, `503.3 КБ` shell JS, `9` lazy chunks); full+adaptive Chromium E2E; secret scan `1113`; history scan `302`; production audit `0 vulnerabilities` и `git diff --check` зелёные. Fingerprint остаётся `fnv1a32:74ce379c`, immutable v1 — `fnv1a32:45cee292`; provider/платных вызовов, install, commit, push и deploy не было.
- Текущий remediation checkpoint заменяет более ранние count-only freeze figures ниже; статус тикета остаётся `in-progress` до двух новых буквальных `ZERO_FINDINGS`. Browser-safe `public/grammar-domain-contract.js` единолично владеет семью error codes, четырьмя active practice types, generated revision/pointer и exact confusion-pair parser; catalog, runner, recorder, server validation и screen используют его без локальных enum/regex/revision copies. Device-local `grammarRunner` не попадает в generic progress/module sync: transport очищает только клон, серверные file/PostgreSQL full-save и merge отбрасывают входящий runner и удаляют прежнюю server-копию, тогда как exact owner-generation local reload сохраняется. Review-remediation RED был буквально зафиксирован как `62 total / 56 pass / 6 fail`; GREEN — `66/66`: active event требует exact `choice/input/correction/transform` и четыре совпадающих score/outcome группы, legacy требует совпадения объявленных типов и scores с фактическим множеством outcomes без missing/extra. OpenAPI требует `16..32` active items, exact four declarations/scores и отдельные строгие builtin/generated legacy item schemas без active-only transfer/diagnostic/confusion metadata; generated legacy provenance требует source/revision. Расширенный Grammar/catalog/mastery/offline/owner/Voice/security контур прошёл `210/210`, отдельный content/property/Voice — `27/27`, полный unit — `1430 total / 1388 pass / 42` штатных PostgreSQL skip / `0 fail`. Disposable PostgreSQL project `easyboost-postgres-integration-3932` применил миграции `001–052`, прошёл `42/42` и полностью удалил container/volume/network; Docker остановлен. Lint; check (`366 JS`, `205` handlers, `123` names); build (`482` assets, `501.6 КБ` shell JS, `9` lazy chunks); full+adaptive Chromium E2E; secret scan `1113`; history scan `302`; production audit `0 vulnerabilities` и `git diff --check` зелёные. Fingerprint остаётся `fnv1a32:74ce379c`, immutable v1 — `fnv1a32:45cee292`; provider/платных вызовов, install, commit, push и deploy не было.
- Пять тем группы «Времена» — `1` Present Simple / Present Continuous, `2` Past Simple / Past Continuous, `3` Present Perfect / Past Simple, `13` Past Perfect и `4` Future forms — содержат ровно 32 активных задания каждая: по 8 `choice`, `input`, `correction` и `transform`, всего 160 семантически уникальных заданий. Неизменяемый `grammar-core-v1` остаётся адресуемым с 200 упражнениями, 18 exam gaps и fingerprint `fnv1a32:45cee292`; текущий `grammar-core-v2` содержит 310 упражнений, 18 exam gaps и намеренно обновлённый fingerprint `fnv1a32:74ce379c`. Неоднозначные v2 формулировки заменены контекстами с одним диагностическим ответом, включая явно заданный Past Perfect, fixed-timetable Present Simple и Past Continuous background action с фиксированным порядком частей; естественные ASCII/типографские contractions принимаются без принятия форм без апострофа. Исторические v1 content/fingerprint не менялись. Ровно 13 дополнительных допустимых порядков обстоятельств зафиксированы буквальными ответами в 10 controlled-transform заданиях; удаление именно этих 13 строк из текущего v2 воспроизводит предыдущий fingerprint `fnv1a32:31ae9e4f`, поэтому иной content delta исключён. Финальный literal-аудит подтвердил 160 уникальных exact prompts, 120 явно классифицированных неправильных choice-вариантов и 546 адресуемых Voice Tutor ссылок двух ревизий (218 v1 + 328 v2).
- Общий runner выбирает детерминированную очередь из 16 исходных заданий (по 4 каждого типа и не более одного задания из каждой авторской пары), проверяет только явно разрешённые варианты и не повторяет элементы. Ограниченный полный поиск гарантированно оставляет уникального unseen peer каждому из 16 выбранных заданий; свойство доказано для пяти тем на 4096 seed каждая (20 480 очередей), а каталог с менее чем 8 заданиями каждого типа или без пар закрывается fail-closed. Все 80 активных авторских пар и 240 направленных weakness outcomes проверены буквально: каждому выбранному wrong outcome соответствует точная слабость в mate. После каждой ошибки исходного задания runner вставляет отдельный unseen transfer той же точной слабости; неправильный transfer не запускает бесконечную цепочку, а сохраняет bounded `due_next_session`. Исходный результат и transfer pointer синхронно сохраняются до любого таймера.
- Открытие правила или автоматический показ правильного ответа после ошибки немедленно замораживает всю сессию как assisted; assisted evidence не продвигает mastery. Choice хранит отдельную серверную диагностику каждого неправильного варианта, но публичный runner раскрывает только выбранную диагностику после ответа. Чистая независимая сессия меняет stage только после ответа canonical mastery API; client-reported metadata не является authority для mastery или adaptive plan. До сетевого enqueue точное полное событие/UUID сохраняется как owner-generation-bound `completion_pending`; reload повторяет его и очищает snapshot только после durable apply/exact replay. Canonical событие атомарно сохраняет версию/ревизию каталога, упорядоченные IDs и bounded outcomes без prompt/answer; сервер назначает `endedAt`, точный replay сравнивает весь material event, а изменённый top-level/session payload с тем же UUID конфликтует. Offline-очередь применяет тот же exact-event контракт. Export включает bounded историю, delete удаляет её.
- Темы `5–12` и `14–20` сохраняют старый выбор/input runner через тот же строгий envelope с каноническим mode `legacy_practice`. Он записывает непустые упорядоченные catalog outcomes и exact UUID, но не притворяется активной четырёхуровневой сессией и не применяет active-only ограничения 4/type и transfer. Ошибка проверяется по server catalog, автоматический разбор делает всю сессию assisted, а каждый исходный legacy item получает не более одного повтора; вторая ошибка закрывается `due_next_session` без нового requeue, поэтому семь исходных заданий дают максимум 14 outcomes. Сгенерированные дополнения входят только через стабильный server-owned pointer `generated.g.q.<requestHash>.<resultDigest>.c/fN@1`: mixed/generated session всегда assisted, snapshot/reload восстанавливает точный pointer, а mastery history хранит только ID/revision/type/outcome без prompt/answer/reference. File и PostgreSQL разрешают pointer внутри той же owner serialization/transaction; cross-owner, неизвестный или удалённый source отклоняется для нового события, тогда как exact уже записанный UUID replay остаётся доступным, а изменённый payload конфликтует до повторного resolve. Чистый topic 5 сохраняет только partial `learning`; assisted wrong не продвигает stage. Оба пути используют durable `completion_pending`, переживают reload/startup flush, exact replay не дублирует history; file/PostgreSQL export/delete сохраняют тот же bounded/no-answer контракт.
- Voice Tutor использует точный revision registry: сохранённые ссылки v1 разрешают 218 исторических bank/exam items, текущие ссылки v2 — 328 items = 310 bank + 18 exam gaps; неизвестная ревизия и v2-only ID с revision 1 закрываются fail-closed. Каждый публичный pointer содержит только `id`/`revision`, а canonical server item разрешается в ту же ревизию и тот же проверяемый ответ, включая correction/transform.
- Browser gate буквально проходит wrong → paired transfer для каждого из четырёх типов, reload/`completion_pending`, assisted state и отдельную чистую сессию до server-authoritative `learned`; также проверяются `aria-live`, клавиатура/фокус и отсутствие horizontal overflow на 320/375/768/1440 px. Фактическая цель кнопки правила при 375 px в трёх последовательных layout-frame и в normal/reduced-motion — `76.671875 × 48` px, computed `min-block-size:48px`, `display:flex`, `flex-shrink:0`, `box-sizing:border-box`. Немедленный reload до 900 ms восстанавливает точные `done`/error/transfer ID; захваченные correct/wrong callbacks не меняют заменённый item или новую сессию. Bootstrap читает workflow только из exact owner-generation envelope, накладывает исключительно `grammarRunner` (включая очищающий `null`) и не принимает локальные mastery/counters как authority. Банки тикетов 04–06 не менялись.
- Freeze gate перед повторным независимым review: active-runner 19/19; расширенный Grammar/catalog/mastery/offline/owner/privacy/Voice контур 163/163; полный unit 1424 total (1382 pass, 42 штатных PostgreSQL skip, 0 fail); финальный live PostgreSQL применил миграции 001–052 и прошёл 42/42. Общий file/PostgreSQL контракт подтверждает 32 упорядоченных active outcome, 16 active `due_next_session`, exact `confusionPair:null`, duplicate-pair reject до записи/no-learned, full-event replay/conflict, legacy clean/wrong/second-wrong-due, а также owner-bound generated pointer/revision, cross-owner и removed-source reject, durable replay/conflict, export/no-content/delete parity. Generated provenance дополнительно проходит write → canonical GET/migrate → unrelated mastery mutation → export: exact ID/source/revision/type/outcome сохраняются, prompt/answer/reference не появляются. Valid builtin, generated `.cN/choice` + `.fN/input` и mixed composition переживают повторную нормализацию/GET/export; suffix/type mismatch и четыре source/assisted contradiction удаляют противоречивую session, не получают replay authority и конфликтуют при same-UUID попытке. Mid-session reload теперь буквально проверен для builtin и generated legacy input: восстановленный `input` сохраняет canonical `word_or_verb_form`, exact generated revision/assisted provenance и проходит server schema. OpenAPI разделяет builtin/generated/mixed legacy composition, требует assisted для generated/mixed, связывает `.cN/.fN` с choice/input, структурно исключает incomplete mixed arrays и связывает top-level event source/assisted с теми же полями вложенной session. Обе последовательности owner-lock race сведены к безопасным исходам: event-first сохраняет событие и последующий replay, removal-first отклоняет новое событие. Зелёные также lint; check (364 JS, 205 handlers, 123 names); frontend build (482 assets, 502.0 КБ shell JS, 9 lazy chunks); full и adaptive Chromium E2E; secret scan 1113 tracked файлов; history scan 302 commits; production audit (0 vulnerabilities); `git diff --check`. Временные PostgreSQL container/volume/network удалены, Docker остановлен; provider/платных вызовов, install, push, deploy и commit не было.
- Зафиксированные RED → GREEN циклы включают: all-wrong симуляцию, где прежний reserve давал только `6/7/8/8/7` transfer на 16 ошибок пяти тем, заменённую гарантией 16/16; same-UUID replay, ранее сравнивавший только `session`, заменённый canonical fingerprint всего события; completion snapshot, ранее очищавшийся до durable enqueue, заменённый `completion_pending`; option-level диагностику вместо статической topic/item слабости; hit-target suite `12/13 → 13/13`; offline exact-UUID suite `0/1 → 1/1`; literal RED `24/28` — history truncation 24/32, повтор авторской пары, terminal punctuation и неверная chosen-option taxonomy — заменён GREEN-контрактами history 32, one-original-per-pair, Unicode-safe optional terminal `.?!` и 120/120 точных choice-диагностик. Последний независимый review дал exact RED `34/38`: темы 5–20 отправляли пустую session, contractions без апострофа вытесняли естественные ответы, тема 13 допускала Past Simple reading, а OpenAPI рекламировал невозможную unassisted wrong session. После GREEN `38/38` дополнительные structural RED `52/53` и два точечных `0/1` закрыли catalog-bound legacy wrong, единый `legacy_practice` mode и reload очереди с повторным legacy item. Следующий свежий review дал bounded RED `42/48`: generated supplement терял стабильный ID/type и не завершался, legacy retry рос без ограничения, `core.g.13.f.7` допускал Past Simple, а общий OpenAPI `uniqueItems` противоречил повтору legacy outcome. GREEN `48/48` и усиленный safety-контур добавили addressable owner-generated refs, максимум один retry с `due_next_session`, однозначный Past Perfect context и раздельные active/legacy session schemas. Последний exact RED `28/33 → 33/33` обнаружил потерю generated `source/revision` при canonical normalization и три неоднозначных v2 input-контекста; GREEN сохраняет только валидный generated pointer/revision, отбрасывает forged metadata на built-in ID и явно требует Past Perfect либо fixed-timetable Present Simple. Финальный P2 дал RED `14/16 → 16/16`: normalization принимал `.cN/input` и `.fN/choice`; теперь обе противоречивые session удаляются fail-closed, не авторизуют replay, а валидные направления сохраняются idempotently в file/PostgreSQL history/export. Последний composition RED `17/21 → 21/21` закрыл builtin+generated, generated+builtin-only, incomplete mixed и unassisted generated; exact source теперь выводится из items, generated/mixed требуют assisted, а shared PG подтверждает валидные builtin/generated/mixed и все конфликтные same-UUID варианты. Финальный reload/OpenAPI RED `27/29 → 30/30` воспроизвёл старый `f` → восстановленный `input` alias и underconstrained generated metadata; GREEN покрывает builtin/generated post-reload wrong completion и структурную provenance matrix публичного контракта. Заключительный RED `18/20 → 20/20` закрыл неоднозначный `core.g.2.transform.1` и независимые top-level/session source/assisted поля OpenAPI; v2 prompt теперь диагностически однозначен, а четыре event-level ветви не допускают противоречивый envelope. Заключительный bounded RED `18/19 → 19/19` добавил 13 конечных грамматичных порядков обстоятельств ровно в 10 controlled-transform заданиях; проверка остаётся literal/exact и не вводит смысловой matcher. Итоговые focused/full/PG gates приведены выше. Тикет остаётся `in-progress` до двух новых буквальных `ZERO_FINDINGS` и одного локального коммита; дерево заморожено на review.
