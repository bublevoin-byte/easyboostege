# 06 — Активный банк служебных слов

Status: done
Blocked by: 03 — Активный runner
Spec: `.scratch/grammar-2/spec.md#implementation-decisions`

## Что сделать

Расширить артикли, предлоги и союзы/связки до полного четырёхуровневого банка, который проверяет выбор по контексту, исправление и контролируемое преобразование, а не только запоминание маркеров.

## Границы

- Входят три названные темы, 72 задания, нулевой артикль, устойчивые предлоги и синтаксические формы связок.
- Не входят остальные 17 тем и смешанный/adaptive selector.

## Файлы

- `public/grammar-catalog-content.js`, `public/grammar-catalog.js` — контент и coverage.
- `public/modules/grammar.js`, `public/screens/grammar.js` — общий runner и due/error focus.
- `test/`, `e2e/` — catalog/domain/browser contracts.

## Definition of Done

- [x] Три темы содержат минимум 72 уникальных задания, по 6 каждого типа на тему.
- [x] Нулевой артикль, устойчивые предлоги и разные синтаксические формы связок покрыты явными ответами.
- [x] Transfer items меняют контекст и не повторяют исходную строку.
- [x] Состояние темы, error focus и due review работают через общий runner.
- [x] Catalog coverage и browser/domain tests проходят.
- [x] `npm test`, `npm run lint` и `npm run check` проходят; один коммит на тикет.

## Verification checkpoint

- Первый независимый Spec review вернул три P2 content finding; каждый seam был зафиксирован до remediation как exact RED `13 total / 10 pass / 3 fail` и закрыт GREEN `13/13`. `core.g.19.c.7` теперь использует однозначную noun phrase `his nervousness`; `core.g.19.transform.3/.4` буквально публикуют принимаемое начало конечной структуры, а `.4` принимает ровно этот instruction-compliant вариант; authored transfer pair `.1/.2` использует два разных исходных контекста.
- Второй независимый Spec review вернул ещё три P2 content finding; их публичные seams были добавлены до production-изменений и дали exact RED `16 total / 13 pass / 3 fail`, затем GREEN `16/16`. `core.g.15.c.7` теперь явно задаёт значение «в течение декабря», `core.g.19.c.7` объясняет именно noun phrase после `despite`, а `core.g.19.transform.6` требует две полные части и публикует принимаемое начало. Повторный расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `76/76`; current fingerprint — `fnv1a32:4a590236`, immutable v1 не изменился.
- Третий независимый Spec review вернул ещё три P2 content finding; их публичные seams дали exact RED `17 total / 14 pass / 3 fail`, затем GREEN `17/17`. `core.g.15.f.5/.6` теперь принимают конечный грамматичный синоним `during`; `core.g.15.c.7` использует однозначный wrong option `of`, поэтому диагностика `time_preposition__non_time_preposition` снова точна; `core.g.19.correction.2` стал обратной cause/result тренировкой `SO → BECAUSE`, а не clause/phrase заданием под чужой weakness. Повторный расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `77/77`; current fingerprint — `fnv1a32:4db8d134`, immutable v1 не изменился.
- Четвёртый независимый Spec review вернул ещё два P2 content finding; публичные seams были добавлены до production-изменений и дали exact RED `19 total / 17 pass / 2 fail`, затем GREEN `19/19`. Активный v2 `core.g.15.c.3` заменил допустимый deadline-вариант `by` на однозначный wrong option `of`, но immutable v1 буквально сохраняет прежние options; открытые connector prompts теперь принимают конечные грамматичные `while`, `still` и `yet` вместе с уже опубликованными эквивалентами. Повторный расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `79/79`; current fingerprint — `fnv1a32:d5892f3f`, immutable v1 остался `fnv1a32:45cee292`.
- Пятый независимый Spec review вернул четыре P2 content finding; их публичные seams были добавлены до production-изменений и дали exact RED `23 total / 19 pass / 4 fail`, затем GREEN `23/23`. `core.g.14.f.2/.4` теперь буквально задают неизвестный слушателю первый mention и institutional normal purpose «as pupils»; `core.g.15.c.8` заменил временной distractor `from` на однозначно нетемпоральный `of`; открытые `core.g.19.f.6/.8` принимают `and yet` и `and therefore`; `core.g.19.correction.2` принимает обе конечные позиции причины с `because`. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `83/83`; current fingerprint — `fnv1a32:d011fa85`, immutable v1 остался `fnv1a32:45cee292`.
- Шестой независимый Spec review вернул ещё три P2 content finding; публичные seams дали exact RED `24 total / 21 pass / 3 fail`, затем GREEN `24/24`. `core.g.14.f.1` теперь явно задаёт первый mention; объяснения `core.g.15.f.5/.6` соответствуют обоим принимаемым `in` и `during`; активный v2 `core.g.19.c.4` заменил синтаксически несовместимый, но смыслово concessive `despite` на грамматичный wrong-relation `or`, сохранив generic exact transfer и неизменный v1. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `84/84`; current fingerprint — `fnv1a32:7c53f945`, immutable v1 остался `fnv1a32:45cee292`.
- Седьмой независимый Spec review вернул четыре P2 content finding; все четыре публичных seam были добавлены до production-изменений и дали exact RED `26 total / 22 pass / 4 fail`, затем GREEN `26/26`. `core.g.14.f.3` теперь буквально отличает обычный приём пищи от scheduled event; `core.g.15.correction.5/.6/.8` прямо требуют конечные `in`/`on`, соответствующие finite grader; открытые `core.g.19.f.2/.7` принимают грамматичные coordinating equivalents `and therefore`/`and yet`; активный v2 `core.g.19.c.5` теперь действительно проверяет cause/result, а не ошибочно описывает синтаксис `despite`, не изменяя immutable v1. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `86/86`; current fingerprint — `fnv1a32:1802160c`, immutable v1 остался `fnv1a32:45cee292`.
- Восьмой независимый Spec review вернул четыре P2 content finding; все четыре публичных seam были добавлены до production-изменений и дали exact RED `30 total / 26 pass / 4 fail`, затем GREEN `30/30`. Активный v2 `core.g.19.c.2/.7` использует только грамматичные noun-phrase cause/result distractors, а `.c.3/.8` — только грамматичные full-clause wrong-relation distractors с точными diagnostics; все восемь connector input буквально публикуют конечное множество принимаемых форм; active `core.g.14.c.1` задаёт listener-new idea, не меняя v1; `core.g.14.correction.3/.4` образуют exact transfer pair `zero_article__indefinite_article` на двух обычных приёмах пищи. Расширенный Grammar/catalog/mastery/offline/owner/Voice контур прошёл `90/90`; current fingerprint — `fnv1a32:b72dadf8`, immutable v1 остался `fnv1a32:45cee292`.
- Девятый независимый Spec review вернул четыре P2 finding; публичные seams дали exact RED `66 total / 62 pass / 4 fail`, затем GREEN `66/66`, а усиленная сквозная проверка сохранённой catalog identity дала отдельный RED `33 total / 32 pass / 1 fail`, затем GREEN `33/33`. Concessive choice explanations теперь называют смысловое отношение вместо ложного синтаксического объяснения; `core.g.14.c.6` и `core.g.14.correction.3` буквально задают нужный article/meal context; `core.g.15.f.1–6` публикуют конечные допустимые временные формы; восстановленная до активации очередь v1 отображает immutable v1 prompt/options/answer и сохраняет точную v1 catalog identity через общий runner/server/OpenAPI контракт. Расширенный Grammar/catalog/domain/mastery/offline/owner/Voice контур прошёл `95/95`; current fingerprint — `fnv1a32:7d4ebc25`, immutable v1 остался `fnv1a32:45cee292`.
- Десятый remediation checkpoint закрывает два P1 и один P2 finding девятого финального Spec review: публичные seams дали exact RED `67 total / 63 pass / 4 fail`, затем GREEN `67/67`. Input prompts тем `15` и `19` сохраняют bounded relation/syntax/structure instruction, но больше не раскрывают принимаемые токены до ответа; `core.g.15.f.7` фиксирует dependent-preposition reading «pay attention» вместо неоднозначных duration/search readings; настоящий pre-Ticket06 v2/revision2 legacy snapshot выбирает immutable historical content index, отображает исходные prompt/options/answer и сохраняет прежнюю v2 catalog identity, в том числе при `completion_pending`. Расширенный Grammar/catalog/domain/mastery/offline/owner/Voice контур прошёл `96/96`; current fingerprint — `fnv1a32:affdc848`, immutable v1 остался `fnv1a32:45cee292`.
- Одиннадцатый remediation checkpoint закрывает P1 finding десятого финального Spec review: public seams дали exact RED `35 total / 32 pass / 3 fail`, затем GREEN `35/35`; дополнительная обычная сезонная форма `over` была зафиксирована отдельным RED `35 total / 34 pass / 1 fail`, затем GREEN `35/35`. Time inputs фиксируют actual occurrence и accountable duty; connector inputs ограничивают rule-card forms либо точный conjunction/adverb type, а все обычные instruction-compliant `whereas`/`whilst`/`yet`/contrast subordinators и сезонный `over` принимаются без answer leakage. Расширенный Grammar/catalog/domain/mastery/offline/owner/Voice контур прошёл `97/97`; current fingerprint — `fnv1a32:c2bac557`, immutable v1 остался `fnv1a32:45cee292`.
- Двенадцатый remediation checkpoint закрывает два P1 и два P2 finding одиннадцатого финального Spec review. Exact public seams были зафиксированы до production как RED `38 total / 33 pass / 5 fail` и закрыты GREEN `38/38`; расширенный affected-контур прошёл `91/91`. Дополнительный public factory seam доказал, что caller мог произвольно выбрать active topics, как RED `6 total / 5 pass / 1 fail`, затем закрыт GREEN `6/6`; финальный focused Grammar/catalog/domain/mastery/offline/owner/Voice контур прошёл `93/93`. Настоящий pre-Ticket06 `grammar-core-v2`/revision `2` теперь является отдельным immutable registry snapshot, current каталог продвинут до `grammar-core-v3`/revision `3`, а общий runner, mastery, persistence и OpenAPI восстанавливают и проверяют точную v1/v2/v3 identity по capability и membership без caller-selectable active-topic bypass. `core.g.15.c.6` больше не называет временной `for` нетемпоральным; `core.g.15.f.2`, `core.g.19.f.3/.5` буквально ограничивают конечную проверяемую rule-card форму и не обещают принятие более широких грамматических категорий.
- Тринадцатый remediation checkpoint закрывает один P1 и три P2 finding двенадцатого финального Spec review. Все четыре публичных seam были добавлены до production-изменений и дали exact RED `40 total / 36 pass / 4 fail`, затем GREEN `40/40`; расширенный Grammar/catalog/domain/mastery/offline/owner/Voice контур прошёл `97/97`. Общий Voice registry теперь хранит не только immutable v1 и current v3, но и все immutable v2 pointers: настоящий неправильный ответ из восстановленной v2-сессии создаёт Voice Tutor attempt с revision `2`, тогда как неизвестная revision по-прежнему fail closed. `core.g.19.f.5` принимает грамматичный single-word contrast adverb `regardless`; объяснение `core.g.19.c.3` описывает опубликованное продолжение работы, а не выдуманное завершение; active-only override `core.g.14.c.5` объясняет конкретную conventional форму `the USA`, не меняя immutable v2 и не обобщая правило на все многословные страны.
- Четырнадцатый remediation checkpoint закрывает P1 finding тринадцатого финального Standards review. Публичные session-bound seams были зафиксированы до production как exact RED `35 total / 33 pass / 2 fail`, затем закрыты GREEN `35/35`; расширенный Grammar/catalog/domain/mastery/offline/owner/Voice контур прошёл `105/105`. Восстановленная активная immutable-v2 сессия теперь получает topic bank, topic metadata и item index именно своей revision: неправильный ответ создаёт v2 transfer, после reload сохраняет revision `2`, открывает точный v2 Voice Tutor pointer и завершает server-valid v2 event. Pair ownership на сервере также разрешается через submitted immutable session catalog, поэтому current-v3 item нельзя подмешать в v2 mastery event.
- Пятнадцатый remediation checkpoint закрывает один P1 Spec и два P2 Standards finding четырнадцатого финального review. Каталог×topic OpenAPI matrix была зафиксирована как exact RED `16 total / 15 pass / 1 fail`: v2 legacy topic `10` воспроизвёл расхождение runtime `false` / OpenAPI `true`, а та же ветка покрывает `10/11/12/16/17/20`; после production GREEN `16/16`. Два новых публичных seam-модуля до implementation дали `0/2` из-за отсутствующих exports, затем целевой контур прошёл `82/82`, расширенный Grammar/catalog/domain/mastery/offline/owner/Voice — `128/128`. Один registry-backed runtime теперь владеет exact version+revision, item lookup, legacy membership и active capability; browser restore и server validation используют его без v1/v2/v3 switches, Voice registry автоматически индексирует каждую immutable revision, а generated OpenAPI связывает каждый historical catalog только с темами, не активными именно в нём: v1 — все девять pre-activation тем, v2 — только `14/15/19`. Regex по имени `sessionCatalogItems` и source-order assertions удалены; behavioral seams проверяют injected immutable/current pair ownership и durable `explain`+transfer snapshot до delayed renderer.
- Шестнадцатый remediation checkpoint закрывает P2 Standards finding пятнадцатого финального review: три оставшиеся ручные OpenAPI ownership-зоны больше не требуют синхронных правок при добавлении immutable каталога. Публичный generator seam до production дал exact RED `1 total / 0 pass / 1 fail` из-за отсутствующего модуля, а generated-document check отдельно завершился ожидаемым stale-контрактом; после реализации focused-контур прошёл `19/19`. Единый registry-derived builder теперь формирует active catalog×topic capability branches, active-session catalog union, legacy-session catalog union и pre-activation legacy ownership. Инъекция synthetic `grammar-core-v4` доказывает автоматическое появление следующей revision, а executable `60`-cell matrix сверяет все `3` зарегистрированные runtime с каждой из `20` тем и обе session unions с runtime capability.
- Тикет закрыт `done` после двух свежих независимых буквальных `ZERO_FINDINGS` на одной frozen identity и одним локальным коммитом. Публичный начальный TDD seam был зафиксирован до production как RED `1 total / 0 pass / 1 fail`: тема `14` ещё имела Grammar 1 coverage `5/0/0/0` вместо требуемых `8/8/8/8`; production закрыл его GREEN `10/10`; текущий focused Grammar/catalog/domain/mastery/offline/owner/Voice контур прошёл `128/128`.
- Темы `14`, `15`, `19` содержат `96` уникальных активных заданий: ровно `32` на тему и `8` каждого из типов `choice`, `input`, `correction`, `transform`. Current `grammar-core-v3`/revision `3` содержит `665` упражнений + `18` exam gaps, всего `683` item ID, fingerprint `fnv1a32:a26bf36f`; immutable `grammar-core-v2`/revision `2` содержит `584 + 18`, fingerprint `fnv1a32:86530c23`; immutable v1 остаётся `200 + 18`, `fnv1a32:45cee292`.
- Exhaustive content gate подтверждает нулевой артикль, устойчивые временные и зависимые предлоги, clause/noun-phrase/punctuation формы связок, отдельную диагностику каждого выбранного wrong option, конечные явно опубликованные эквиваленты и exact authored transfer, меняющий контекст. Общий Ticket03 runner, catalog/domain/mastery/offline/owner/OpenAPI/Voice контракты используются без тематических ветвей; immutable v1 pre-activation и v2 active/pre-Ticket06 membership допускают только соответствующие исторические очереди, а current v3 — полный active банк.
- Финальный последовательный unit/integration suite прошёл `1527 total / 1485 pass / 42` штатных PostgreSQL skip / `0 fail`. Предыдущие FINAL14 stale source assertion и два известных несвязанных default-concurrency timing transient вместе с exact isolated `21/21` и последовательным green сохранены в evidence, а не скрыты. Lint; generated OpenAPI check; check (`376 JS`, `205` handlers, `123` names); build (`482` assets, `614.4 КБ` shell JS, `9` lazy chunks); full и adaptive Chromium E2E; secret scan `1124`; history scan `305`; свежий production audit `0 vulnerabilities` и `git diff --check` зелёные.
- Первый disposable PostgreSQL прогон применил миграции `001–052`, но завершился `39/42`: три прежних Voice Tutor сценария с entitlement на точной временной границе получили `VOICE_TUTOR_PREMIUM_REQUIRED`. Без изменений кода exact elevated rerun на новом disposable project снова применил `001–052` и прошёл `42/42`; оба прогона полностью удалили container/volume/network, а независимые post-run filters не нашли остаточных project resources. Этот transient результат сохранён в evidence, а не скрыт.
- После первой content remediation disposable PostgreSQL project `easyboost-postgres-integration-36532` заново применил миграции `001–052`, прошёл `42/42` и полностью удалил container/volume/network. После второй remediation финальный project `easyboost-postgres-integration-34472` снова применил `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые filters пусты. Один unit-прогон под одновременной нагрузкой получил прежний Speaking timing failure `service_hung`; тот же тест сразу прошёл изолированно `11/11`. Позднее один длинный chained-прогон получил несвязанный startup/seed transient Task Bank (`3 !== 6`); `test/task-bank.test.js` сразу прошёл изолированно `10/10`, затем полный suite был зелёным. Первый post-doc полный прогон с обычной файловой конкуренцией одновременно повторил оба несвязанных timing transient (`service_hung` и Task Bank `5 !== 6`), `1450 pass / 2 fail / 42 skip`; изолированные перепроверки сразу прошли `11/11` и `10/10`, а новый полный прогон с `--test-concurrency=1` прошёл `1494 total / 1452 pass / 42 skip / 0 fail`. Все transient результаты сохранены в evidence, а не скрыты.
- После третьей remediation финальный disposable PostgreSQL project `easyboost-postgres-integration-24220` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые filters пусты. Новый полный последовательный suite прошёл `1495 total / 1453 pass / 42 skip / 0 fail` без повторения прежних transient.
- После четвёртой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-31056` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые filters контейнеров, томов и сетей пусты.
- После пятой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-30440` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые filters контейнеров, томов и сетей пусты.
- После шестой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-30516` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые filters контейнеров, томов и сетей пусты.
- После седьмой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-24476` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые elevated filters контейнеров, томов и сетей пусты.
- После восьмой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-35336` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые elevated filters контейнеров, томов и сетей пусты.
- После девятой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-28056` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые elevated filters контейнеров, томов и сетей пусты.
- После десятой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-37548` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые elevated filters контейнеров, томов и сетей пусты.
- После одиннадцатой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-37928` заново применил миграции `001–052`, прошёл `42/42`; runner удалил container/volume/network, независимые elevated filters контейнеров, томов и сетей пусты.
- После двенадцатой remediation disposable PostgreSQL project `easyboost-postgres-integration-39444` применил миграции `001–052`, прошёл `42/42` и полностью удалил container/volume/network. После усиления общего persistence contract точными v2/v3 assertions новый project `easyboost-postgres-integration-41248` снова применил `001–052`, прошёл `42/42`; его container/volume/network также полностью удалены. Docker после проверок снова остановлен.
- После тринадцатой remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-3364` заново применил миграции `001–052`, прошёл `42/42`; runner полностью удалил container/volume/network, а независимые elevated filters всех трёх типов пусты. Docker после проверки снова остановлен.
- После четырнадцатой session-bound remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-6604` заново применил миграции `001–052`, прошёл `42/42`; runner полностью удалил container/volume/network, независимые elevated filters всех трёх типов пусты. Docker после проверки снова остановлен.
- После пятнадцатой registry/OpenAPI remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-9528` заново применил миграции `001–052`, прошёл `42/42`; runner полностью удалил container/volume/network, независимые elevated filters всех трёх типов пусты. Docker после проверки снова остановлен.
- После шестнадцатой registry-generated OpenAPI remediation свежий disposable PostgreSQL project `easyboost-postgres-integration-30680` заново применил миграции `001–052`, прошёл `42/42`; runner полностью удалил container/volume/network, независимые elevated filters всех трёх типов пусты. Docker после проверки снова остановлен.
- Docker остановлен. Provider/платных вызовов, package install, commit, push и deploy не было; Ticket07 не начинался.
