# 07 — Смешанная практика и точный фокус плана

Status: done
Blocked by: 04 — глагольные конструкции; 05 — части речи; 06 — служебные слова
Spec: `.scratch/grammar-2/spec.md#implementation-decisions`

## Что сделать

Собрать все 20 тем в смешанные и targeted sessions без подсказки названием темы. История показывает конкретные слабости и пары-конфликты, а индивидуальный план назначает точную тему/error focus с учётом срока ЕГЭ, не пропуская доказательные интервалы.

## Границы

- Входят mixed selector, targeted selection, weak-skill history, adaptive recommendation, offline/persistence/export/delete parity.
- Не входят расширение тематических банков и финальная интеграция режима 19–24.

## Файлы

- `public/modules/grammar.js`, `public/screens/grammar.js` — mixed/targeted runner и UI.
- `adaptive/`, `storage/file-repository.js`, `storage/postgres-repository.js`, `public/sync.js` — plan focus и durable state.
- `test/`, `e2e/` — selection, adaptive, offline и parity contracts.

## Definition of Done

- [x] Mixed selector балансирует темы, типы, новизну и слабости и не раскрывает ответ названием экрана.
- [x] Recommendation выбирает exact topic/error/confusion focus, имеет server-owned pointer и не доверяет client substitution.
- [x] Deadline pressure может назначить раннюю практику, но не продвигает stage до eligibleAt.
- [x] Generated/AI items проходят строгий quarantine и не могут быть sole mastery evidence.
- [x] Reload/offline sync/adaptive overview/file/PostgreSQL/export/delete tests проходят.
- [x] `npm test`, `npm run lint` и `npm run check` проходят; один коммит на тикет.

## Текущий checkpoint

Final closeout (supersedes the historical checkpoints below): Ticket07 is `done`. The final affected Grammar/mastery/offline/OpenAPI contour is GREEN `91/91`; compact full unit completed with exit `0` across `1552` tests (`1510` pass + `42` expected PostgreSQL skips). Disposable PostgreSQL project `easyboost-postgres-integration-36912` applied migrations `001–052`, passed `42/42`, and cleaned container/volume/network. Lint, generated OpenAPI check, check (`380 JS`, `208` handlers, `125` names), build (`482` assets, `629.4 KB`, `9` lazy chunks), full+adaptive Chromium E2E, secrets (`1128`), history (`306`), and diff-check are green. With explicit authorization for npm registry metadata disclosure, fresh `npm audit --omit=dev --audit-level=high` completed with exit `0` and `0 vulnerabilities`; `package.json` and `package-lock.json` remained unchanged. Fresh independent Standards and Spec reviews both returned literal `ZERO_FINDINGS` on canonical identity `2f2195e518a99740a7e98258f7c69252ed8cdfe79aee270309c9530df226c9e3` at base `6a7a2488e9ef9ddb5efd8c026ce3151261fc8f4e`. One local commit closes the ticket; Docker is stopped, and no provider/paid calls, package install, push, deploy, or Ticket08 work occurred.

Latest mixed-response identity remediation (supersedes the historical checkpoints below): status remains `in-progress` until a fresh dependency audit, two fresh independent literal `ZERO_FINDINGS` reviews on one frozen identity, and one local commit. Standards review found that the legacy result-without-topic fallback still accepted an ambiguous mixed result. Exact reconnect RED was `3 total / 2 pass / 1 fail`; it is GREEN `3/3`, and the affected Grammar/mastery/offline/OpenAPI contour passes `91/91`. A topic-less result is authority only for an ordinary entry without `topicExpectations`; mixed results must name a declared physical topic or remain queued after the bounded retry without any broadcast. Compact full unit completed with exit `0` across `1552` tests (`1510` pass + `42` expected PostgreSQL skips). Lint, generated OpenAPI check, check (`380 JS`, `208` handlers, `125` names), build (`482` assets, `629.4 KB`, `9` lazy chunks), full+adaptive Chromium E2E, secrets (`1128`), history (`306`), and diff-check are green. Disposable PostgreSQL project `easyboost-postgres-integration-36912` remains applicable because later work does not touch server validation or repositories; it applied migrations `001–052`, passed `42/42`, and cleaned container/volume/network. Fresh `npm audit` remains blocked by environment policy for external dependency metadata; package manifests are unchanged and Ticket06 audit evidence is not claimed as fresh. Docker is stopped; no provider/paid calls, package install, commit, push, deploy, or Ticket08 work occurred.

Latest fail-closed review remediation (supersedes both historical checkpoints below): status remains `in-progress` until a fresh dependency audit, two fresh independent literal `ZERO_FINDINGS` reviews on one frozen identity, and one local commit. Standards review found a sole wrong non-null `topicId` response could use a same-event fallback and clear another queued topic. Exact reconnect RED was `2 total / 1 pass / 1 fail`; it is GREEN `2/2`, and the affected Grammar/mastery/offline/OpenAPI contour passes `90/90`. Queue authority now requires exact `(eventId, topicId)` or the documented legacy result without a topic, while broadcast accepts only the queued topic or declared mixed `topicExpectations`; a mismatch remains queued after the existing bounded retry and is never broadcast. Compact full unit completed with exit `0` across `1551` tests (`1509` pass + `42` expected PostgreSQL skips). Lint, generated OpenAPI check, check (`380 JS`, `208` handlers, `125` names), build (`482` assets, `629.3 KB`, `9` lazy chunks), full+adaptive Chromium E2E, secrets (`1128`), history (`306`), and diff-check are green. Disposable PostgreSQL project `easyboost-postgres-integration-36912` remains applicable because later changes do not touch server validation or repositories; it applied migrations `001–052`, passed `42/42`, and cleaned container/volume/network. Fresh `npm audit` remains blocked by environment policy for external dependency metadata; package manifests are unchanged and Ticket06 audit evidence is not claimed as fresh. Docker is stopped; no provider/paid calls, package install, commit, push, deploy, or Ticket08 work occurred.

Latest review remediation checkpoint (supersedes the historical figures below): status remains `in-progress` until a fresh dependency audit, two fresh independent literal `ZERO_FINDINGS` reviews on one frozen identity, and one local commit. Standards/Spec review found two public contract defects: offline reconnect collapsed expanded mixed results by shared `eventId`, and batch `200/201` OpenAPI responses omitted the runtime owner-binding header. Exact RED was `66 total / 64 pass / 2 fail`; after remediation the affected Grammar/mastery/offline/OpenAPI contour passes `89/89`. Queue resolution now uses `(eventId, topicId)`, cross-tab sync publishes each authoritative physical topic record exactly once, and both successful batch responses document `X-EasyBoost-Response-Owner`. Full unit is `1550 total / 1508 pass / 42` expected PostgreSQL skips / `0 fail`; lint, generated OpenAPI check, check (`380 JS`, `208` handlers, `125` names), build (`482` assets, `629.1 KB`, `9` lazy chunks), full+adaptive Chromium E2E, secrets (`1128`), history (`306`), and diff-check are green. Disposable PostgreSQL project `easyboost-postgres-integration-36912` remains applicable because post-PG changes do not touch server validation or repositories; it applied migrations `001–052`, passed `42/42`, and cleaned container/volume/network. Fresh `npm audit` remains blocked by environment policy for external dependency metadata; package manifests are unchanged and Ticket06 audit evidence is not claimed as fresh. Docker is stopped; no provider/paid calls, package install, commit, push, deploy, or Ticket08 work occurred.

Реализация и все доступные без внешней сети gates готовы, но тикет остаётся `in-progress` до свежего dependency audit, двух независимых `ZERO_FINDINGS` на одной frozen identity и одного локального коммита. Первый review выявил три смысловых пробела и одно дублирование контракта; exact RED был `30 total / 23 pass / 7 fail`. Следующий review нашёл повторное применение старого signed receipt на новой mastery revision и умножение одной mixed-ошибки копиями полной сессии в per-topic history; новый exact RED был `15 total / 12 pass / 3 fail`. После remediation текущий focused-контур проходит `76/76`: HMAC receipt дополнительно связан с exact `expectedRevision`, а server recommendation и local mixed selector учитывают каждый mixed outcome только в записи его фактической темы. Mixed completion одной owner-bound транзакцией применяет exact topic expectations: ранняя практика только пишет историю, due unassisted recall может продвинуть каждую фактическую тему, а поздняя exact ошибка регрессирует только её тему. Targeted selector fail-closed требует минимум `2` exact и `4` error-family items, а завершение связано с owner, pointer, revision и ordered `8` IDs и безопасно восстанавливается offline. Полный unit — `1549 total / 1507 pass / 42` штатных PostgreSQL skip / `0 fail`. Свежий disposable PostgreSQL project `easyboost-postgres-integration-36912` применил миграции `001–052`, прошёл `42/42`, включая обновлённый shared Grammar file/PostgreSQL persistence/export/delete contract, и в `finally` удалил container, volume и network; последующая remediation не меняла validation или repositories, поэтому этот прогон остаётся применим. Lint, generated OpenAPI check, syntax/inline-handler check (`380 JS`, `208` handlers, `125` names), build (`482` assets, `628.8 КБ`, `9` lazy chunks), full+adaptive Chromium E2E, secrets (`1128`), history (`306`) и diff-check зелёные. Свежий `npm audit` не запускался повторно: запрос внешних dependency metadata был отклонён политикой среды; `package.json` и `package-lock.json` не менялись, а прежний audit Ticket06 не выдаётся за свежий. Docker остановлен; provider/платных вызовов, package install, commit, push и deploy не было; Ticket08 не начинался.
