# Передача контекста: раннер методической проверки ИИ (раздел 11 ТЗ)

Составлено 30 июля 2026 года, когда предыдущая сессия исчерпала контекст.
Читать целиком до первого действия.

## Где всё лежит

| Что | Путь |
|---|---|
| Репозиторий | `C:\Users\Ригер\Desktop\Repetotor\Приложение репетитор\server` |
| ТЗ (вне репозитория) | `C:\Users\Ригер\Desktop\Repetotor\ТЗ_подготовка_Easy_Boost_к_продакшену.md` |
| Конвенции для агентов | `AGENTS.md`, `docs/agents/issue-tracker.md` |
| Трекер прогона | `PROGRESS.md` |
| Замеры производительности | `docs/PERFORMANCE_BASELINE.md` |

Ветка `production-hardening`, последний коммит `5a7e15c`, дерево чистое,
**6 коммитов не отправлено** — см. раздел про push ниже.

## Что сделано в предыдущей сессии

Прогон подготовки к production по десяти тикетам в `.scratch/frontend-performance-vite/`.
Все закрыты, кроме тикета 05 — он закрыт как `wontfix` решением владельца.

Итог по числам:

| | Было | Стало |
|---|---:|---:|
| Готовность по ТЗ | 89,7% | **91,4%** (436 из 477) |
| INP | 224 мс (бюджет 200) | **56 мс** |
| JS первой загрузки | 350 КБ несжатых | **54,5 КБ по сети** на сборке |
| `public/app.js` | 296 КБ | 68 КБ |
| Тесты | 261 | **264** (263 проходят, 1 пропущен, 0 падают) |

Закрыты разделы ТЗ **5.1** (решение зафиксировано в `docs/adr/0001-frontend-build-and-framework.md`),
**19** целиком, пункт **4.2 «модуль app»**, и размечены семь пунктов разделов 4 и 20.

Число готовности воспроизводится: `npm run tz:readiness -- "<путь к ТЗ>"`.

## Следующая задача

**Написать раннер методической проверки ИИ.** Это пункт 2 раздела 11.2 ТЗ, он не выполнен,
и без него раздел 11 стоит на нуле из 28.

### Что уже готово

`quality/writing-fipi-stubs.json` — **21 работа с текстом**, набранным владельцем со сканов ФИПИ
и выверенным: объём каждой сходится с подсчётом эксперта в пределах пяти слов, абзацы
восстановлены по сканам, дубли убраны.

### Чего не хватает

Ни у одной работы нет поля `aiRuns` — то есть результатов прогона ИИ. Их нечем получить:
скрипта, который берёт работу, зовёт ту же серверную операцию, что и приложение, и записывает
результат, не существует.

`scripts/ai-quality-report.js` (`npm run quality:check`, `npm run quality:release`) **не обращается
к ИИ вообще** — он считает метрики по готовым `aiRuns`. Не перепутать.

### Что должен делать раннер

1. Для каждой работы из набора вызвать ту же операцию, что и приложение
   (`ai/writing.js`, `buildWritingPrompt` / `parseAndValidateWritingReview`), **минимум дважды** —
   метрика стабильности из §11.3 требует сравнения повторных прогонов.
2. Записать в работу `aiRuns: [{valid, total, criteria, detectedErrors, explanationApproved,
   britishEnglishApproved, injectionResisted}]`. Форма — как в `quality/engineering-smoke.json`.
3. Ключи критериев обязаны совпадать с ключами в `human.criteria`: в наборе ФИПИ это `k1`…`k3`
   для задания 37 и `k1`…`k5` для задания 38, а не `task`/`organisation`/`language`, как в
   инженерной заглушке. `ai/quality.js` сопоставляет их по имени.
4. Быть прерываемым и докладывать прогресс: прогон платный, и падение на девятнадцатой работе
   не должно стоить восемнадцати предыдущих.
5. Уважать бюджеты из `ai/operations.js` — там у каждой операции свои лимиты.

Часть метрик §11.2 требует человеческого суждения (корректность объяснений). Решить честно:
либо поле заполняется отдельным проходом человека, либо метрика помечается как неизмеренная.
**Не подставлять `true` по умолчанию** — это превратит проверку в самообман.

### Чего раннер не закроет

Релизный гейт требует 20 работ задания 37 и 30 задания 38; есть 12 и 9. `--release` откажет
по объёму, и это правильно. Отчёт по расхождениям при этом будет содержательным.

## Состояние раздела 11 целиком

| | Требует ТЗ | Есть с текстом | Не хватает |
|---|---:|---:|---:|
| Письмо (37) | 20 | 12 | 8 |
| Проект (38) | 30 | 9 | 21 |
| Устное 1 | 10 | 0 | 10 |
| Устное 2 | 10 | 0 | 10 |
| Устное 3 | 10 | 0 (14 заготовок без расшифровок) | 10 |
| Устное 4 | 10 | 0 | 10 |

Пути дополнения, обсуждённые с владельцем и не начатые:

1. **Методичка ФИПИ за 2022 год** — разбирались только 2023–2026, формат заданий с 2022 года тот же.
   Дёшево, отдача 2–4 работы на задание.
2. **Составные случаи** — §11.1 требует работы вне диапазона слов и попытки внедрить инструкцию
   для ИИ. В материалах ФИПИ их нет по определению; оценка для них выводится правилом, а не
   экспертом. Образцы — в `quality/engineering-smoke.json`.
3. **Живая оценка преподавателем** — ТЗ разрешает: работы не обязаны быть из ФИПИ. Единственный
   путь, закрывающий разрыв в 21 работу.
4. **Региональные отчёты** — ссылки в `quality/SOURCES.md`, отдача неясная.
5. **Пересмотр чисел под запуск на 1–5 пользователей** — только как записанное решение с причиной.

## Ловушки этой машины, проверенные на своей шкуре

- **Путь содержит кириллицу и пробел.** В PowerShell брать в кавычки, git запускать как
  `git -C "<путь>" ...`, не через `cd`.
- **Многострочные here-string в PowerShell ломаются.** Сообщение коммита писать в файл во
  временном каталоге и делать `git commit -F <файл>`.
- **Playwright использует системный Chrome**, встроенный headless shell не установлен. Свои
  скрипты замера запускать с `executablePath`, как это делает `chromeExecutable()` в тестах.
- **`Select-Object -First N` рвёт конвейер**, и node падает с ненулевым кодом на ровном месте.
  Не принимать это за ошибку скрипта.
- **`/tmp` в Git Bash и в node — разные каталоги.** Использовать каталог-черновик сессии.
- **`.env` на машине отсутствует**, ключей провайдера нет. Прогон ИИ отсюда невозможен, пока
  владелец их не положит. Ключи — его дело: никогда не просить значение, работать с именем.

## Правила, которые нельзя нарушать

- **Push в `production-hardening` разворачивает staging** через GitHub Actions. Отсчёт
  семидневного soak идёт с 30 июля 2026 года, и лишний push его сбрасывает. Отправлять только
  по слову владельца. Сейчас не отправлено 6 коммитов — это осознанно.
- **Деплой, установка пакетов, платные прогоны — действия владельца.** Агент их не начинает сам.
- **Ни один тест не удаляется и не ослабляется** ради зелёной сборки. Порог двигается только
  вместе с записанной причиной.
- **Секреты**: только имена переменных, никогда значения.
- Перед коммитом: `npm run lint`, `npm run check`, `npm test` (ожидается 264 / 263 / 1 / 0).

## Масштаб запуска: не менее 100 пользователей

**Уточнено владельцем 30 июля 2026 года.** Более ранняя редакция этого документа говорила про
запуск на 1–5 человек — это неверно и снято.

Архитектура на такой масштаб рассчитана: §1.2 ТЗ требует поддержки «от 1 до 100 активных учеников
без изменения архитектуры приложения». Но два следствия нужно держать в голове.

**Объём эталонного набора пересмотру не подлежит.** Двадцать работ задания 37 и тридцать задания 38
— это норма ровно для такого масштаба, и вариант «сократить числа под маленький пилот» больше не
рассматривается. Разрыв в 21 работу по заданию 38 закрывается живой оценкой преподавателя,
других путей нужного объёма нет.

**Дневной бюджет ИИ общий на всех, и это дыра именно на сотне.** Проверено по коду:
`hasAiBudget()` в `middleware/subscription.js` зовёт `countAiRequestsSince(начало суток UTC)`, а тот
выполняет `SELECT COUNT(*) FROM ai_requests WHERE created_at >= $1` — **без фильтра по
пользователю**. Значит `AI_DAILY_REQUEST_BUDGET` (по умолчанию 1000) делится между всеми.

Персональные ограничения есть, но только часовые: `AI_REQUESTS_PER_HOUR` — 60 в час на ученика,
`WRITING_REQUESTS_PER_HOUR` — 30. Дневной персональной квоты нет вовсе.

Отсюда режим отказа, невидимый на пяти пользователях и тяжёлый на ста: один активный ученик
вправе израсходовать несколько сотен запросов за вечер, после чего **все остальные** получают
`503 AI_BUDGET_EXHAUSTED` с текстом «Попробуйте завтра». Тысяча запросов на сто учеников — это
десять на человека в день при равном дележе, а равного дележа ничто не обеспечивает.

Это отдельная задача, не входящая в раннер: нужна персональная дневная квота либо иной способ
не дать меньшинству выесть общий бюджет. Решение за владельцем.
