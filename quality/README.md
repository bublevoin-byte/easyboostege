# AI quality dataset

`engineering-smoke.json` проверяет только расчёт метрик и защитные сценарии. Он не является методической экспертизой.

Для release-набора создайте отдельный JSON по той же структуре. Каждая запись должна содержать анонимный ответ, эталонные баллы и идентификатор квалифицированного проверяющего в `human.reviewer`, результаты повторных AI-прогонов и теги сложности. Не добавляйте ФИО или Telegram ID.

Минимальный release-состав: writing 37 — 20, writing 38 — 30, speaking 1–4 — по 10. Команда `npm run quality:release -- quality/release.json` отклонит неполный набор или метрики ниже порогов ТЗ.

## Что уже собрано

`speaking-3-fipi.json` — 14 полных интервью (задание 3) из методических материалов ФИПИ
2023–2026 годов, собранных командой `npm run quality:extract` из `quality/sources/`.
В каждой записи пять вопросов, расшифровка ответа, побалльная оценка эксперта по каждому
вопросу и его комментарий. Минимум раздела 11.1 для этого задания — 10, он перекрыт.

Разметку эксперта прямо в расшифровке (`knowLEX`, `wasGR`, фонетические скобки, замечания
по-русски) скрипт вырезает: её не произносил ученик, и модель не должна её видеть. Тест
`test/quality-speaking-dataset.test.js` не пропустит кириллицу в расшифровке, несогласованный
итог и потерю уровней сложности.

Набор пока не запускается через `quality:check`: в записях пустой `aiRuns`. Это ожидаемо —
эталонные оценки собраны, прогоны модели нужно записать отдельно.

Открытыми остаются письменные работы 37 и 38 (у ФИПИ это сканы бланков, нужен ручной набор)
и устные задания 1, 2 и 4 (в материалах они разбираются по аудиозаписям). Подробности и
ссылки — в `SOURCES.md`.
